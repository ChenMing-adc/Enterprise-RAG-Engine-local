# ==========================================
#  RAG 私有知识库引擎 依赖清单
# ==========================================

# --- 1. 核心 Web 框架与环境管理 ---
streamlit==1.32.0             # 构建可视化交互网页
python-dotenv==1.0.1          # 管理 .env 环境变量

# --- 2. 大模型与 RAG 编排框架 (LangChain 生态) ---
langchain==0.1.13
langchain-core==0.1.33
langchain-community==0.0.29
langchain-openai==0.1.1       # 兼容 OpenAI 协议 (对接 DeepSeek)
langchain-text-splitters==0.0.1
langchain-huggingface==0.0.1  # 加载本地 HuggingFace 嵌入模型 (M3E)

# --- 3. 向量数据库 (持久化存储) ---
chromadb==0.4.24              # 轻量级本地向量数据库

# --- 4. 混合检索与重排引擎 ---
sentence-transformers==2.5.1  # 提供 Cross-Encoder 重排模型
rank_bm25==0.2.2              # BM25 稀疏检索核心算法

# --- 5. 文档解析引擎 (公式/表格提取) ---
llama-parse==0.4.0            # LlamaIndex 视觉多模态文档解析器
nest-asyncio==1.6.0           # 解决异步事件循环冲突